# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next
out
build
next-env.d.ts
*.tsbuildinfo

# environment - never commit real secrets
.env
.env.local
.env*.local
.env.production
.env.development

# source media, regenerated by scripts/harvest-assets.mjs (110 MB)
assets/raw

# editor
.vscode
.idea

# testing
coverage
/playwright-report
/test-results

# deploy
.vercel

# dashboard uploads in development (src/lib/uploads.ts). On the server they
# live in a Docker volume, never in the repository.
/uploads

# Country lookup database for the visitor statistics, downloaded by
# scripts/fetch-geo.mjs (npm run geo:fetch). About 8 MB, refreshed monthly,
# and fetched into the image at build time, so never committed.
/geo

# misc
*.log
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
*.pem
snapshots
